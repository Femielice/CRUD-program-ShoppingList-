package shoppinglist.entity;

public class DaftarBelanjaDao {

    public boolean insert(DaftarBelanja daftarBelanja);

    public boolean update(DaftarBelanja daftarBelanja);

    public boolean delete(DaftarBelanja daftarBelanja);
}
